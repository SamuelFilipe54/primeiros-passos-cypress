# My first automation with Cypress

This project uses Cypress to automate the login feature tests and the user update feature.

## Installation
``` bash
npm install
```
it is necessary to have node installed

## Running
In the git terminal, use the command below:
``` bash
npx cypress open
```
Run the automation with:
``` bash
npx cypress run
```
